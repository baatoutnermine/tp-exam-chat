{
  "name": "tp-exam-chat-backend",
  "version": "1.0.0",
  "description": "Mini chat backend - Express API",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "node -e \"const app = require('./server'); console.log('✅ Server loaded successfully'); process.exit(0);\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
